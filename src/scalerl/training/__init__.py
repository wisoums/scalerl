"""Deep-RL training pipelines (Stable-Baselines3).

Import the concrete modules, e.g. ``scalerl.training.dqn``; this package does
not import them, so ``python -m scalerl.training.dqn`` runs cleanly. Training
code never imports MLflow or Optuna at import time.
"""
